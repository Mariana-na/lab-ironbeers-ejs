<% data.forEach(beer => { %>
    <div class="beer">
      <img src="<%= beer.image_url %>" alt="Beer Image">
      <h2><%= beer.name %></h2>
      <p><%= beer.description %></p>
      <p><%= beer.tagline %></p>
    </div>
  <% }); %>